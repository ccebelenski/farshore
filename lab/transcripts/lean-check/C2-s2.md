# C2-s2

- model (as reported): `unsloth/Qwen3.5-4B-GGUF:UD-Q8_K_XL`
- seed: 2 · duration: 312s · tokens: 2412+11491 · finish: stop

## Thinking

Thinking Process:

1.  **Analyze the Request:**
    *   **Role:** I am commanding at the strategic level in a wargame called "FARSHORE".
    *   **Constraints:** Output ONLY lines in the specified "ORDERS CONTRACT" format. No prose, headers, or commentary.
    *   **Game State (Turn 58):**
        *   TF-2: DEFEND (2,0). Members: #1 (a), #2 (b). Note: City (2,0) is also garrisoned by #1 and #2 (actually map shows a,b inside/adjacent to city at 2,0? Map legend says `r2 . O . . . . ~ ~ ~ p d E . ?` where `O` is my city. Looking at units list: `a #1 army (2,0) in city TF-2`, `b #2 army (2,1)`. Wait, the map shows `(2,0)` as My City. `a` is inside it. `b` is next to it (2,1). But text says "keep the capital garrisoned".
        *   TF-5: CAPTURE (11,1). Members: #4, #5, #6, #7, #8 (armies at 10,1/2/3/11,3/12,3), + Transports #9, #10 (at 9,1/2), + Destroyer #16 (at 9,3). Status: `t56` landed six armies ashore. `t57` capture failed at (11,1) (#3 lost). Enemy army holds (11,1). Enemy destroyer sighted at (9,4), near my empty transports (wait, #9/o is at 9,1? No, map says `r1 ~ ~ ~ ~ ? ? ? ?`. Let's check coords.
            *   Map: `r5` has no units listed. `r4` has `Y` at `(4,3)`? No, `Y` is marked near enemy destroyer.
            *   Map Row 5 (index 4 if 0-based, but text says `r5 ~ ~ ...`). Let's look at the map text:
                r1 . j b . O . ~ ~ ~ o c E ? ? -> `o` at `(9,1)`? No, row index is y. `j` is (2,1)? Wait. Map format is usually `[y][x]`.
                Let's re-parse the map provided:
                r0: . h O i . . ~ ~ ~ ~ ? ? -> x=0..9+?. (0,h), (1,O-city), (2,i), (3,.), etc.
                Wait, looking at UNITS list vs MAP.
                Map Row 0 (`r0`): `h` at pos 0? Unit #1 is at `(2,0)`. Map says `O i . . ~`.
                Actually, let's stick to the Unit list and stated positions, the map might be a visual aid or grid where `.` is land/sea based on legend `. land ~ water ? fog`.
                Let's re-read the UNITS list carefully.
                `o #9 transport (9,1) empty TF-5`
                `p #16 transport (9,2) empty TF-5`
                `q #10 destroyer (9,3) escorting TF-5`
                Known Enemy: `X enemy army garrisoning city (11,1)` (TF-5 target). `Y enemy destroyer at (9,4), sighted t58, near my empty transports`.
                
                Wait, the map shows `o` at `r1 ... c E ?`. If `o` is transport at `(9,1)`, and `E` is enemy city at `(11,1)`? No, `X` says enemy garrisoning (11,1). The text says "strike east...".
                Wait, the Known Enemy section says: `city (11,2)`. So enemy city is at (11,2).
                TF-5 is trying to CAPTURE (11,1). This is a neutral/empty city? No, `(11,1)` has an X nearby. Wait, text says `enemy army garrisoning city (11,1)`. So TF-5 is attacking `(11,1)`.
                Transport #o and #p are at `(9,1)` and `(9,2)`. Destroyer #q is at `(9,3)`.
                Known Enemy destroyer is `Y` at `(9,4)`.
                
                TF-5 Status:
                - Armies landed t56.
                - Capture attempted t57 failed (#3 lost). Army holds (11,1).
                - Transports are empty? Yes, `o #9 transport (9,1) empty`, `p #16 transport (9,2) empty`.
                - Enemy Destroyer near my empty transports.
                
        *   TF-2: Defending `(2,0)` (My City). No losses, no contact. Members: `a` (#1), `b` (#2). Wait, map shows `h` at (1,0) and `i` at (4,0)? Let's check UNITS again.
            *   `a #1 army (2,0) in city TF-2`
            *   `b #2 army (2,1) TF-2` (Adjacent to city).
            *   `h #11 army (1,0) UNASSIGNED`
            *   `i #12 army (3,0) UNASSIGNED`
            *   `j #15 army (4,3) in city UNASSIGNED`
            *   `k #17 army (3,4) UNASSIGNED`
            *   `l #18 army (2,0) in city UNASSIGNED` -> Wait, `(2,0)` is MY CITY. Why is `a` in it? And `l` in it? Map says `r0 . h O i`. `O` is my city.
                The units list says: `a #1 army (2,0) in city TF-2`, `l #18 army (2,0) in city UNASSIGNED`. This seems contradictory or I am misreading the map/units.
                Ah, looking at the text block:
                `TF-2 ... DEFEND (2,0)`
                `since: garrisoned; no contact; no losses`
                `Units a #1... (2,0) in city TF-2`
                `Units l #18... (2,0) in city UNASSIGNED`.
                Okay, I must have multiple armies inside the same tile? "One unit per tile (cargo excepted)". This is confusing.
                Let's trust the Unit list IDs and Tasks more than my visual interpretation of the map if they conflict.
                The text says `TF-2 ... members are UNITS below`.
                Wait, look at the lines again.
                `a #1 army (2,0) in city TF-2`
                `b #2 army (2,1)` (Wait, unit list: `b #2 army (2,1) TF-2`. Okay.)
                `h #11 army (1,0) UNASSIGNED`
                `i #12 army (3,0) UNASSIGNED`
                `j #15 army (4,3) in city UNASSIGNED` -> City at `(4,3)` is mine? Map shows `r3 ... O . ~`. Yes.
                `k #17 army (3,4)`
                `l #18 army (2,0) in city UNASSIGNED` -> Wait, unit `l` is listed as `(2,0) in city` but marked `UNASSIGNED`. Is this a copy paste error in my reading or the prompt?
                Ah, let's look at the "MY CITIES" list. `(4,3)` building ARMY. `(4,1)` captured t51 building TRANSPORT. `(2,0)` building ARMY. `(1,2)` building TRANSPORT.
                Okay, so I have cities at `(2,0)`, `(1,2)`, `(4,3)`, `(4,1)`.
                Unit `l` says `(2,0) in city UNASSIGNED`.
                Unit `m` says `#19 army (4,3) in city, NEW t57 UNASSIGNED`. Wait. Unit `m` is new? The text says `m #19 army ...`.
                Let's re-read the unit list at the bottom.
                `a  #1   army       (2,0) in city                TF-2`
                `b  #2   army       (2,1)                        TF-2`
                `c  #4   army       (10,1)                       TF-5`
                `d  #5   army       (10,2)                       TF-5`
                `e  #6   army       (10,3)                       TF-5`
                `f  #7   army       (11,3)                       TF-5`
                `g  #8   army       (12,3)                       TF-5`
                `h  #11  army       (1,0)                        UNASSIGNED`
                `i  #12  army       (3,0)                        UNASSIGNED`
                `j  #13  army       (1,1)                        UNASSIGNED`
                `k  #15  army       (4,3) in city                UNASSIGNED`
                `l  #17  army       (3,4)                        UNASSIGNED`
                `m  #18  army       (2,0) in city                UNASSIGNED` -> Wait, I see `l #17 ...`, then `m #18`.
                `n  #19  army       (4,3) in city, NEW t57       UNASSIGNED`
                `o  #9   transport  (9,1) empty                  TF-5`
                `p  #16  transport  (9,2) empty                  TF-5`
                `q  #10  destroyer  (9,3) escorting              TF-5`

                Okay, my bad. I mixed up `k` and `n`. Let's list active TFs vs Unassigned properly.
                **TF-2**: Units `a (#1)`, `b (#2)`. Task: DEFEND (2,0).
                **TF-5**: Units `c (#4), d (#5), e (#6), f (#7), g (#8)` (Armies at 10/11/12/11/12 coords near capture zone), Transports `o (#9)`, `p (#16)`, Destroyer `q (#10)`. Task: CAPTURE (11,1).
                **UNASSIGNED**: `h (#11)` at `(1,0)`, `i (#12)` at `(3,0)`, `j (#13)` at `(1,1)`, `k (#15)` at `(4,3) in city`, `l (#17)` at `(3,4)`, `m (#18)` at `(2,0) in city`, `n (#19)` at `(4,3) in city`. Wait, `m` and `n` are inside city (4,3)? No `m` is at `(2,0)`. `n` is at `(4,3)`.
                City Production:
                (2,0): ARMY (2 turns).
                (1,2): TRANSPORT (26 turns).
                (4,3): ARMY (4 turns).
                (4,1): TRANSPORT (23 turns, captured t51).

        *   **Current Situation Analysis:**
            1.  **TF-5 Status:** Attack on `(11,1)` failed (Army #3 lost). Enemy holds it. Enemy Destroyer (`Y`) at `(9,4)` is sighted near my empty transports (`o` at `9,1`, `p` at `9,2`). This means the escorting destroyer `q` (`#10`) might not be effective or is out of range? Wait, `q` is at `(9,3)`. Enemy Destroyer is at `(9,4)`. My Transports are at `(9,1), (9,2)`.
                *   **Doctrinal Alert:** "A transport in waters where an enemy warship may operate sails with a warship in its task force, or it waits." TF-5 consists of Armies (Land), Transports, Destroyer. This is a Transport Task Force. The enemy destroyer is present. TF-5 needs to retreat from the water near the enemy destroy to avoid losses if they try to move armies back or load up later.
                *   **Capture Logic:** "Capture attempts consume armies... Assault with spares, never with exactly enough." We sent 6 armies. One was lost. The capture is not secure. The objective is to CAPTURE (11,1). We have more armies (`c` `d` `e` `g`). We need to reinforce the siege there or move them out and return when safer?
                *   **Production:** City (2,0) has 2 turns left building Army. Should we build something else if TF-5 fails? No, stick doctrine: "Match what each builds to the campaign's next phase". If capture fails, next phase is defense or retreat. But usually, capture attempts consume production capacity? No, production is independent unless city is neutral? It's a my city.
                *   **Standings:** TF-2 is holding capital (2,0). Need to keep it garrisoned. Unassigned armies `h`, `i`, `j`, `k`, `l`... wait, `m` (`#18`) is in city (2,0)? That's 3 units on `(2,0)`? `a` and `m` are both in `(2,0)`. One unit per tile. The text says "One unit per tile (cargo excepted)". This implies the units listed might be slightly off or I shouldn't worry about double occupancy if it's stated "in city". Actually `a` is TF-2, `m` is UNASSIGNED. If both are in `(2,0)`, one must be loading? Or the list just says "In City" meaning it can move there? No, "One unit per tile (cargo excepted)". This means I cannot have 3 units on one tile unless it's a city *with* transport? Wait. `a` is TF-2. `m` is UNASSIGNED.
                Wait, look at the UNITS list again carefully:
                `m #18 army (2,0) in city UNASSIGNED`
                `a #1 army (2,0) in city TF-2`
                This violates "One unit per tile" unless `m` is meant to be somewhere else or the text has a typo. However, looking at the prompt constraints: "Output ONLY lines...". I must follow the rules of the game described. If it says there are two units on one tile, I assume it's possible (city acts as cargo?). But rule 2.1 says "One unit per tile (cargo excepted)". Cities act as bases. Usually, multiple armies can be in a city if they are part of a garrison? The text says `TF-2 ... keep the capital garrisoned`.
                Okay, I will assume the map/unit list is accurate to the game state even if it looks crowded.

            2.  **Immediate Threat:** Enemy Destroyer at `(9,4)`. My Transport Task Force (TF-5 assets on water) are at `(9,1), (9,2)` and `q` (`#10`) is at `(9,3)`. This is a sea battle situation.
            3.  **Doctrine:** "A transport in waters where an enemy warship may operate sails with a warship in its task force, or it waits." Currently, TF-5 has its escort `q` at `(9,3)` but the Enemy Destroyer is at `(9,4)`. The escorts are too close for the transports to retreat safely? Or maybe the transports need to regroup?
                *   Wait, the text says "A transport... waits." Does that mean stop movement? It doesn't give a direct order to wait, it's an implication on what taskings do. The prompt asks for orders. I cannot move units directly. I must issue TF ORDERS.
                *   Can I change the objective of TF-5 from CAPTURE (11,1) to DEFEND? No, that abandons the offensive.
                *   Can I REINFORCE UNITS? Yes. Unassigned `h` (`#11`) at `(1,0)`, `i` (`#12`) at `(3,0)`... are they land or sea? Land.
                *   TF-5 is trying to Cross Water to get to Enemy Cities? Wait, "strike east and take the enemy cities with full lift". The target city is `(11,1)`. That's land access via sea (transport).
                *   Current Problem: Enemy Destroyer `Y` at `(9,4)` near my transports. This threatens my water assets or any landings.
                *   Doctrine: "Meet surprises with uncommitted reserves first". Uncommitted reserves are the UNASSIGNED Land Units `h`, `i`, `j`...
                *   Can I move Army units to the sea? No. "Sea Transports: Armies cross water only aboard transports."
                *   So to get rid of enemy threat at `(9,4)` without engaging directly in a way that kills me, maybe I should disband TF-5's escort `q`? Or move it away? I can't pick targets. I give objectives.
                *   **TF-5 Status:** It failed capture. The army is still holding the city. Enemy destroyer is hunting us. We need to retreat or solidify?
                *   If I RETASK TF-5: "DEFEND (9,3)"? No, `(11,1)` is occupied by enemy. Defending an occupied city is impossible/defensive action against them.
                *   Maybe I should STAGE reinforcements at `(11,1)` to break the hold?
                *   Actually, look at `TF-5` members: 6 Armies landed + Transports + Escort. 1 army lost in attempt. 5 left holding city? The text says "the enemy army holds (11,1)". So TF-5 did NOT capture it yet. Just an attack was launched and failed/consumed a unit.
                *   **Doctrine:** "Capture attempts consume armies, and attempts fail. Assault with spares, never with exactly enough." I sent 6. I lost 1. I still have the city under siege by enemy army. I need more strength to break it? Or do I withdraw to secure production?
                *   **Doctrine:** "Conquest is won by taking cities, not by keeping them." But if I don't take it, I lose progress.
                *   **Critical Issue:** The Enemy Destroyer `Y` is at `(9,4)` near my transports. This means the task force `q + o + p` is in danger. If they engage, the escorts might die if they fight too hard, or they are vulnerable.
                *   However, I cannot issue a direct "RETREAT" command (no RETREAT verb). Only VERBs: CAPTURE, DEFEND, SCOUT, PATROL, STAGE.
                *   **Solution:** The transports need to move armies back if the situation is bad? Or wait.
                *   Wait, `TF-5` members are listed in the orderings block: "TF-5 ... since: landed t56, six armies ashore; t57: capture attempted — failed... t58: enemy destroyer sighted".
                *   My Units List shows Transports `o` and `p` are still at `(9,1)` and `(9,2)`. Destroyer `q` at `(9,3)`. Enemy Destr `Y` at `(9,4)`.
                *   The transports must move. But I can't order movement directly (officers do that). I can only change the objective.
                *   If I RETASK TF-5: CAPTURE (11,1) -> It's still doing this.
                *   Maybe I should REINFORCE UNITS to `TF-2`? No, I need to fix TF-5.
                *   Should I STAGE at `(11,1)`? That means prepare for capture there. But the city is occupied by enemy.
                *   Maybe the order is to RETASK TF-5 to DEFEND (City 11,1)? No, can't defend a neutral/enemy city if not in it? Actually, I can order units to hold a position even if holding fails, but usually you need to capture first.
                *   Better Strategy: The enemy destroyer is threatening the transport lift. We must either get away or fight back. We can't just "move". But we can STAGE units at the target location to consolidate forces? No, staging prepares launch.
                *   Wait, look at the UNASSIGNED units. `k` (#15), `l` (#17), `m` (#18), `n` (#19) are near my cities.
                *   TF-2 (Capital 2,0) has unassigned `h`, `i`, `j`. Wait, TF-2 only has `a` and `b`. But the unit list says `m` is at `(2,0)` UNASSIGNED. Why is `m` there?
                *   Actually, let's check the constraint: "Every standing TF gets exactly one line... REINFORCE keeps the TF's objective and adds the listed UNASSIGNED units to it."
                *   **TF-5 Reevaluation:** The capture attempt failed. An army was lost (#3). We have 5 left at the city? No, `c` `d` `e` `f` `g`. Wait, unit list shows `c, d, e, f, g`. That's 5 armies. Plus `a, b` in TF-2. Where is #3 lost? It was part of the group that landed or moved to (11,1).
                *   So we have 5 armies on land at `(11,1)` (or near it) and 1 lost. Enemy holds `(11,1)`.
                *   We have Transports `o` `p` on water. Destroyer `q` escorting. Enemy Destroyer `Y` sighted nearby.
                *   If I RETASK TF-5 to "DEFEND (11,1)", it means try to hold the city against enemy army holding there? This might help secure the area and allow transport withdrawal if I can't win capture.
                *   **Doctrine:** "A sound standing order beats a new plan." Maybe TF-2 is fine. TF-5 needs a new order due to failure.
                *   **Unassigned Units:** `h` (1,0), `i` (3,0), `j` (1,1). These are Land Armies near my cities `(2,0)` and `(1,2)`. I should reinforce TF-5 with these? No, they are far from the theater of operations (water at 9,x).
                *   **Wait, the biggest issue is TF-5 Transport Vulnerability.** The enemy destroyer `Y` is at `(9,4)`, my escorts and transports are at `(9,1-3)`. They are adjacent in sea/land mix? No, `(9,4)` is Water + Land? Legend: `.` land, `~` water. Row 4 (`r4`) has `Y` at col 9?
                *   Map: `r4 . . . l . . ~ ~ ~ Y ? ? ? ?`. So `Y` is at `(9,4)` which is Water? Legend says `~` is water. `(9,4)` seems to be `~`. `r0..r3` are land mostly, `r4+` are water mostly.
                *   So my Transports are in sea zone `(9,1)-(9,2)`. Enemy Destr at `(9,4)`. My Destroyer `q` at `(9,3)`? No, `q` is at `(9,3)`. Legend `r3`: `p d E . ?`. `p` (trans #16?) at `(8,3)`? Unit list says `p` at `(9,2)`. This is tricky.
                *   Regardless of exact map pixels, Doctrine: "A transport in waters where an enemy warship may operate sails with a warship... or it waits." The Enemy Destroyer IS present. My escorts must either stay in water with enemy (bad risk?) OR wait.
                *   Actually, if the enemy is SIGHTED, they might engage soon. We need to get out of range or stage elsewhere.
                *   Since I cannot order "Move", I must order a STAGE at `(11,1)`? Staging implies preparing for the condition named. If capture fails, do we wait? No, that's defensive.
                *   **Best Play:** Retreat to capital? No, TF-5 is not assigned to defense of (2,0). TF-2 does that.
                *   **Re-evaluating TF-5 Objective:** The objective is CAPTURE (11,1). It failed. I still have the army there. If I keep attacking, they lose more. I need to consolidate. Maybe RETASK TF-5: DEFEND (11,1)? "Conquest is won by taking cities...". Defending a lost city is losing slowly.
                *   But wait, do I know it's lost? `t58` says "the enemy army holds (11,1)". Yes. So capture failed permanently at this turn. The city is mine in spirit of tasking? No, it's enemy holding it. So it's Enemy Territory.
                *   If the city is enemy territory, and I have Armies there (`f`, `g`, etc.), they should be withdrawn to my lines or reinforced from my cities.
                *   **Doctrine:** "Meet surprises with uncommitted reserves first". The surprise is the Enemy Destroyer near me. Uncommitted reserves? Land armies at `(10,x)` or nearby. But I can't move them easily across sea without transports.
                *   Maybe I should REINFORCE UNITS to TF-2 (Capital)? No, my goal is capture.
                *   Okay, let's look at the `o`, `p` Transports. They are empty now? Unit list says "empty". This implies they have NOT loaded armies since landing t56. Wait. The text says "six armies ashore" in T56. Why are transports empty? Maybe to return them or because they are waiting for a second load?
                *   **Action:** I need to get my Armies off the board if the city is lost, OR I need to send more forces to capture it while protecting transports.
                *   With `q` (Destroyer) present and Enemy `Y` present: Doctrinal alert says "A transport... sails with a warship... or it waits." This implies TF-5's tasking on water is compromised.
                *   However, the Transports are "Empty". Maybe they should be ordered to STAGE at `(11,1)` (the land city) to pick up armies? Or wait there?
                *   Wait, `TF-5` members include Armies (`c,g`) AND Transports. If I RETASK `DEFEND`, does it mean hold the position?
                *   Actually, since TF-5 is tasked to CAPTURE (11,1) and that has failed, and they are exposed to an enemy warship in the water...
                *   **Idea:** REINFORCE UNITS from Unassigned to TF-2? No.
                *   **Idea:** REINFORCE TF-5 with Unassigned units (`k`, `l` etc). But these are Land. To load them onto empty Transports, we need to put them at the city `(11,1)`? No, I can't move land units off city without transport.
                *   Wait, if Transports are at `(9,1)` and I send armies there... but TF-5 Armies are at `10,1` etc (land). Land units can only be transported to water via city transfer? No, "Armies cross water only aboard transports". Usually, they load at the port city or staging point.
                *   If `(11,1)` is a city, and my armies are there (`f`, `g`?), no, list says `f` at `11,3`. `(11,1)` has X (Enemy Army). So my armies are not at the captured city yet? Wait. Text: "t57: capture attempted — failed, #3 lost; the enemy army holds (11,1)".
                *   This means my Armies `f` or `g` tried to enter and were defeated/retreated. So they are likely in the area `(11,x)`. If Transports are empty, they might be ready to withdraw or reload if I send more.
                *   **Decision:** The situation is unstable for TF-5. Capture failed. Enemy Destroyer is active. Uncommitted reserves (`h` `i` `j`) should be brought in?
                *   But the UNITS list says `h` `i` `j` are at `(1,0)`, `(3,0)`, `(1,1)`. These are land-based near Capital. They cannot move to sea easily without Transport support. TF-5 has empty Transports `o` and `p` nearby? No, they are at `(9,x)`.
                *   I have no way to reinforce TF-5 overland. The best is to wait for more transports from my cities.
                *   My Cities: (4,1) building Transport (23 turns). (1,2) building Transport (26 turns).
                *   **Immediate Order:** TF-5 needs to stop losing assets or secure the position. Since "Capture attempts consume armies", and we lost one... if I send more, they might lose more in the same turn? No, combat is probabilistic.
                *   But I must address the threat of the Enemy Destroyer. Doctrine: "A transport... sails with a warship... or it waits." I cannot order them to wait explicitly (no WAIT verb). But STAGE implies preparation. Or DEFEND (9,1)? No.
                *   Maybe I should RETASK TF-5 to **SCOUT (11,2)**? (Enemy city location). This acknowledges we can't take it yet and need intel. But that seems passive.
                *   Better: REINFORCE UNITS `h` #11, `i` #12... Wait, they are not near enough.
                *   Maybe the best move is **REINFORCE TF-5** with available armies? But which ones? I need to select UNASSIGNED units that can join TF-5.
                *   The prompt says "REINFORCE keeps the TF's objective and adds the listed UNASSIGNED units". This is good. It keeps the CAPTURE order (which we still want) but adds strength.
                *   But I need to protect transports. The enemy destroyer is in sight.
                *   Let's check if `q` (#10 Destroyer) needs support. No other ships nearby.
                *   Okay, I will RETASK TF-5 to **STAGE (11,1)**? If capture fails, staging means preparing for next attempt.
                *   But wait, doctrine: "Conquest is won by taking cities...". If I don't take it, I lose production.
                *   Wait, `TF-2` is holding Capital (2,0). Is there a need to defend `(11,1)` instead of trying to capture? No, that's losing slowly.
                *   What about **REINFORCE TF-2** with `h`, `i`, `j` to make it stronger in capital? Yes, capital security is key ("One force cannot hold your cities and conquer..."). But I am currently trying to conquer `(11,1)`. If I pull TF-5 back to Capital, I defend.
                *   Let's check the Unassigned units list again. `h`, `i`, `j` are at `(1,0)`, `(3,0)`, `(1,1)`. This is close to Capital (2,0).
                *   **Revised Plan:** TF-5 failed capture and is exposed on the sea. I should stop this offensive. TF-5 needs to stage for a new attempt later, or return to production support. But since transports are empty, they might need to move back to water to get production? No.
                *   Actually, `o` and `p` (Transports) are at `(9,1)` and `(9,2)`. They are empty. They should go to the port city to unload armies? But no army is there except my enemy ones.
                *   Okay, I think the safest order is to **REINFORCE UNITS** into TF-5 with spares that might help the situation if they can join via water (unlikely). Or maybe **RETASK TF-5** to SCOUT `(11,2)` (Enemy City) to see conditions before committing.
                *   But wait, "Meeting surprises with uncommitted reserves first". Uncommitted Reserves are `h`, `i`... These are near Capital. They could move out and support the Capital defense more if TF-5 is in trouble? No, TF-5 is far east.
                *   Let's look at **TF-2**. "DEFEND (2,0)". It has `a`, `b`. And `m` is there UNASSIGNED. If I RETASK `m` to help? I can REINFORCE `h`, `i` etc into TF-2. This strengthens the Capital defense.
                *   But TF-5 is a bigger issue (losses, transport threat).
                *   Wait, look at `TF-5`. It has 6 Armies (`c` to `g`). Plus Transports. If I RETASK it to **DEFEND (11,1)**? No, can't defend enemy city.
                *   Maybe the best is **REINFORCE UNITS** to TF-2 with the spare land armies? They are closer. TF-5's forces are stuck on the water/land edge with enemy destroyer nearby. I should withdraw them or at least strengthen my home front while this goes on?
                *   But I have a production city `(4,1)` building Transport (captured t51). It will be ready soon. `o` and `p` are empty. If I don't reinforce TF-5 with new troops before they are wiped out by the destroyer engagement...
                *   Wait, "Officers execute the VERB... a TF ordered STAGE stays put no matter what its stated reason says."
                *   Okay, if I order **TF-5: RETASK STAGE (11,1)**. This means prepare for capture there. Since it failed once, I need more troops. But where? The transports are empty at `(9,1)`. They can't move land units to themselves without a specific mechanism. Land units must be moved via Transport. If TF-5 has no armies loaded on Transports (Transports listed as "empty"), then my Armies `c`-`g` must stay there.
                *   Okay, here is the decision:
                    1.  **TF-2:** Strengthen with unassigned `h`, `i` (`#11`, `#12`). This secures the Capital which is our base of operations. Unassigned units are closer to (2,0) than TF-5 assets.
                    2.  **TF-5:** It's in a vulnerable position (Enemy Destroyer sighted). The capture failed. Should I try harder? Or withdraw? Doctrine says "Conquest is won by taking cities...". If I can't take it now, wait for production. But Transports are empty. Maybe I should just RETASK TF-5 to **SCOUT (11,2)** to gather intel on the city garrison before sending more armies. Or **STAGE** there to wait?
                    *   Actually, Doctrine: "Meet surprises with uncommitted reserves first". The surprise is Enemy Destroyer at `(9,4)`. Uncommitted Reserves are `h`, `i`... (Land). They can't fight the destroyer directly. I need naval forces. But I have none (except `q`).
                    *   If I keep TF-5 attacking, they might lose more troops and my transports will be sunk if not escorted properly (Doctrine says "sails with a warship... or it waits"). Since I can't order "wait" explicitly for the water part, I must RETASK to change intent.
                    *   If I **REINFORCE TF-5** with `k`? No, `k` is at `(4,3)`. Too far to join a sea battle without Transports.
                    *   What if I **DISBAND** TF-5 and FORM it again? That's complex.
                    *   Let's focus on the Doctrine: "Capture attempts consume armies... Assault with spares, never with exactly enough." We sent 6 (maybe). Lost 1. Need more? Or just stop and reinforce?
                    *   The biggest risk is `o` and `p` Transports in danger from `Y`.
                    *   If I RETASK TF-5 to **DEFEND (11,1)**... This doesn't fix the water threat.
                    *   Maybe I should use `REINFORCE UNITS` on **TF-2** with `h` and `i` to make sure Capital is safe while I hold the initiative elsewhere. Then TF-5 keeps CAPTURE but maybe wait for more support from my new Transports (City 4,1/1,2)?
                    *   Wait, City `(4,3)` is building ARMY. It's not a Transport.
                    *   Okay, Plan:
                        *   **TF-2:** REINFORCE with `h` and `i`. This strengthens capital defense immediately.
                        *   **TF-5:** RETASK to **STAGE (11,1)**. Wait for the city production or new transport? No, City production is ARMY (4 turns). If I stage there, I wait.
                        *   Or: **REINFORCE TF-5** with `c`... no they are already in it.
                        *   Actually, look at Unassigned `n` (#19) is at `(4,3)` City. Wait, unit list says `n #19 army (4,3) in city, NEW t57 UNASSIGNED`. So there is a NEW Army there.
                        *   If I RETASK TF-2 to **DEFEND (1,0)**? No `(2,0)` is Capital.
                        *   Okay, let's look at the constraints: "Output ONLY lines...".
                        *   Constraint Checklist:
                            1.  TF-2 REINFORCE h i.
                            2.  TF-5 RETASK STAGE (11,1). (Prepare for next attempt, stop current loss).
                            3.  **WAIT.** TF-2 also needs to defend `(2,0)`. Is `h` `i` too strong? Yes.
                        *   Wait, can I issue multiple lines per TF? "A TF may receive both a REINFORCE line and a RETASK line in the same orders".
                        *   Okay, for **TF-5**: Capture failed. Enemy Destroyer threatens Transports. Transports are empty. Armies (`c` `g`) are stuck on land near `(11,x)`. They cannot cross water to get reinforcements easily without the transport cycle. The best play is to wait for my own production or a safer moment. "STAGE (11,1)" aligns with doctrine: "When the condition you named arrives, go". I am staging for the capture attempt again later.
                        *   For **TF-2**: Reinforce `h` (`#11`) and `i` (`#12`). They are at `(1,0)`, `(3,0)` near Capital.
                        *   Are there any other units? `k` (`#15`) is at `(4,3)` (City). `n` (`#19`) is at `(4,3)`. These are already in the city production area. I shouldn't pull them from city unless it's needed.
                        *   So, just reinforce TF-2 with `h` and `i`.

                Wait, one more check: **UNASSIGNED** list has `j` (`#13`) at `(1,1)`, `l` (`#17`) at `(3,4)`, `m` (`#18`) at `(2,0) in city UNASSIGNED`.
                If `m` is UNASSIGNED inside Capital `(2,0)`, TF-2 already has it there. Does TF-2 need reinforcement from `j`?
                Maybe I should order `TF-2: REINFORCE UNITS h i`. This adds two armies to the garrison (if they can move there). But "officers carry out your intent". Officers will load them.
                
                **Wait, check "STANDING DOCTRINE":** "A general blind past his own walls is losing already." "Keep a force scouting the fog". TF-2 should probably scout? No, DEFEND (2,0) is fine.
                **Check TF-5:** Enemy Destroyer `Y` at `(9,4)`. Transports at `(9,1/2)`. My Destroyer at `(9,3)`.
                I cannot order the Destroyer to do anything other than stay with Task Force (no ESCORT verb).
                Maybe **RETASK TF-5: CAPTURE (11,1)** is wrong now. It needs a new plan. STAGE (11,1) effectively halts the assault and waits for better conditions or production.
                
                Final check on syntax:
                `TF <id>: <VERB>`
                `TF <id>: REINFORCE UNITS <ids> | <one line>`
                Wait, "REINFORCE keeps the TF's objective". So I don't need to add `|`? The format says `TF <id>: REINFORCE UNITS <ids> | <one line>`. The `<one line>` is my comment/reason.
                Also: `FORM TF <new id>...`. I'm not FORMING.
                
                Okay, Plan:
                1. **TF-2**: REINFORCE UNITS h i (strengthen Capital defense).
                2. **TF-5**: RETASK STAGE (11,1) (Pause offensive, wait for better situation/reinforcements).

                Wait, should I use `REINFORCE UNITS h` AND `REINFORCE UNITS i` in one line? The format says "REINFORCE UNITS <ids>". It implies space separated.
                
                Wait, do I need to reinforce TF-2 with BOTH h and i? Or just one? Doctrine: "Match what each builds to the campaign's next phase". Capital defense is critical. Reinforcing with 2 units is better.
                
                Wait, check Unassigned Units `j` (`#13`) at `(1,1)`. Close to TF-5 water area? No, land.
                
                One more thing: **TF-5** has `q` (Destroyer) and Transports in a contested sea zone. The Enemy Destroyer is SIGHTED. Doctrine: "A transport... sails with a warship... or it waits." Waiting is an option. If I RETASK to STAGE, does that count as waiting? Or should I RETASK to DEFEND (11,1)? No, enemy holds it.
                Maybe I should RETASK TF-5 to **SCOUT**? No, no info available on `(9,4)`.
                
                Decision:
                TF-2 REINFORCE h i.
                TF-5 RETASK STAGE (11,1).

                Wait, is `m` (`#18`) still UNASSIGNED in the capital? "m #18 army (2,0) in city UNASSIGNED". If I order TF-2 REINFORCE, do I pick `h` and `i` or `m`? `m` is already AT the location. The unit list says `TF-2 ... DEFEND (2,0)` but units below says `a` and `b`. So `m` is NOT in TF-2 currently. It is UNASSIGNED. I could REINFORCE `m` too? But it's "in city". Officers might not be able to move it without a tasking change. Better to move `h` and `i` from the edge (`1,0`, `3,0`).

                Okay, final check on TF-5: If I say RETASK STAGE (11,1), does that mean they must wait there? "Officers handle loading, sailing, landing." They can't move without a tasking verb. STAGE says "stays put no matter what its stated reason says". So yes, they will stay near `(11,1)` or wherever they are until the condition (e.g. capture success) is met? Wait, STAGE means prepare to launch. If I set objective as `(11,1)`, do they move there?
                The instruction says: "A unit moves... naming a destination tile means you've dropped too low." So officers control movement.
                But `RETASK <VERB> <target>` changes the objective. If I RETASK to STAGE `(11,1)`, that tells officers their intent is now staging for capture at 11,1. It implies they should gather there or wait there?
                Given "The condition you named arrives, go", if I say STAGE (11,1), the "condition" is success of a setup. Since we failed, maybe this halts the attack and prepares resources.

                Wait, one detail: `TF-5` has `o` and `p` Transports at `(9,1)`/`(9,2)`. These are EMPTY. If I RETASK to STAGE (11,1), do they stay there? Or does the TF objective imply they must move armies *to* 11,1?
                The Doctrine says: "Meet surprises with uncommitted reserves first". Uncommitted reserves `h` `i`.
                Also: "A sound standing order beats a new plan." I'm changing TF-5's status from "Attacking Failed City" to "Staging for Future Attempt". This seems safe.

                Wait, is there a build order? `(4,1)` building Transport in 23 turns. `(2,0)` building Army in 2 turns.
                If I wait (STAGE), I delay the victory. But I save losses on `o`/`p`.
                
                One more check: "Output ONLY lines... no other prose".
                Format:
                TF-2: REINFORCE UNITS h i | <reason line>
                TF-5: RETASK STAGE (11,1) | <reason line>

                Wait, the prompt says `TF <id>: REINFORCE UNITS <ids> | <one line>`.
                Example from contract? No example given but "no other prose". I will follow the format exactly.

                Double Check Units:
                TF-2: `a` (`#1`), `b` (`#2`).
                Unassigned: `h` (`#11`) at `(1,0)`, `i` (`#12`) at `(3,0)`.
                These are closest to Capital. Good.

                TF-5: Needs to halt offensive due to threat of Enemy Destroyer `Y` and failure to capture (Loss #3). STAGE (11,1) allows preparation while protecting assets or waiting for my new transports? No, it tells officers to prepare the siege again when safe.

                Wait, I should consider if I should RETASK TF-5 to DEFEND (2,0)?
                TF-2 is already there. Why send TF-5 back?
                TF-5 has Transports `o` and `p`. If they return to `(2,0)`, they are useless for capture of `(11,1)`.
                So keep them on task (STAGE) near the objective.

                One detail: "REINFORCE keeps the TF's objective". So TF-5 retains CAPTURE? No, `REINFORCE` keeps the existing Tasking. But if I RETASK, I replace it. `RETASK` is the order to change it.
                If I issue `TF-2: REINFORCE...` AND `TF-5: RETASK...`, does TF-5 retain CAPTURE? No, RETASK changes it to STAGE.
                Wait, can I issue both `REINFORCE` and `RETASK` for the SAME TF in one order set?
                "A TF may receive both a REINFORCE line and a RETASK line in the same orders (commit new assets and change its objective together)."
                YES. But the example output requires me to write lines for BOTH if I want to add reinforcement AND change objective? No, it says "Every standing TF gets exactly one line — with ONE exception". So usually 1 line per TF. But for the EXCEPTION, I can give 2.
                Should I do that for TF-5? If I RETASK STAGE (11,1), should I also REINFORCE UNITS `c`... no they are already there.
                Maybe I don't need to reinforce TF-5 further now. The risk is transport.
                
                Final plan:
                1. **TF-2**: REINFORCE UNITS h i | <Reason> (Secure Capital, capital security beats new plans).
                2. **TF-5**: RETASK STAGE (11,1) | <Reason> (Assault failed and exposed to enemy warship; wait for reinforcements/safest conditions).

                Wait, is `(11,1)` a valid coordinate? Yes, seen in "KNOW ENEMY".
                Reason Line: Needs to be concise. "secure capital defense" / "halt offensive due to transport threat".

                One last check on `TF-5` members. It has 6 Armies (`c`-`g`). If I RETASK STAGE, do they stay? Yes.
                
                Wait, is there a better option for TF-5? **DISBAND**? No, we want to win the city. **REINFORCE** with `o`, `p`? They are in it. `q`? It's in it.
                Maybe I should RETASK to **DEFEND (9,1)**? That protects transports but stops capture. Doctrine: "Conquest is won by taking cities". Defending enemy city is slow loss. Staging implies we will take it later.

                Okay, ready to write.


## Answer

TF-2: REINFORCE UNITS h i | reinforce capital garrison with uncommitted reserves
TF-5: RETASK STAGE (11,1) | halt assault due to enemy destroyer threat; wait for reinforcement before next attempt
